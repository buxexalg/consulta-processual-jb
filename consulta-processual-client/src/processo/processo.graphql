type Processo {
  # Example field (placeholder)
  exampleField: Int
}

input CreateProcessoInput {
  # Example field (placeholder)
  exampleField: Int
}

input UpdateProcessoInput {
  id: Int!
}

type Query {
  processo: [Processo]!
  processo(id: Int!): Processo
}

type Mutation {
  createProcesso(createProcessoInput: CreateProcessoInput!): Processo!
  updateProcesso(updateProcessoInput: UpdateProcessoInput!): Processo!
  removeProcesso(id: Int!): Processo
}
